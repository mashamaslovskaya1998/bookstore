export enum routes {
  // Home = "/",
  SIGN_IN = "/sign-in",
  SIGN_UP = "/sign-up",
  NOT_FOUND = "*",
  FAVORITE_BOOK = "/favoriteBook",
  CARD = "/card",
  USER = "/user",
  SEARCH = "/search",
  NEWBOOK = "/",
  DETAILS_BOOK = "/books/:id",
  SEARCH_BOOK_RESULT = "/search/:title/:page",
}
