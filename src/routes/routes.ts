export enum routes {
  home = "/",
  sign_in = "/sign-in",
  sign_up = "/sign-up",
  // not_found = "*",
  favoritebook = "/favoriteBook",
  card = "/card",
  user = "/user",
  search = "/search",
  newbook = "/",
  details_book = "/books/:id",
  search_book_results = "/search/:title/:page",
}
