export enum routes {
  // Home = "/",
  SIGN_IN = "/bookstore/sign-in",
  SIGN_UP = "/bookstore/sign-up",
  NOT_FOUND = "*",
  FAVORITE_BOOK = "/bookstore/favoriteBook",
  CARD = "/bookstore/card",
  USER = "/bookstore/user",
  SEARCH = "/bookstore/search",
  NEWBOOK = "/bookstore",
  DETAILS_BOOK = "/bookstore/books/:id",
  SEARCH_BOOK_RESULT = "/bookstore/search/:title/:page",
}
